{% extends 'base.html.twig' %}

{% block title %}List des formmations{% endblock %}

{% block body %}
    <h3>Liste des formations</h3>
    <table class='uk-table uk-table-hover uk-table-divider'>
        <thead>
            <tr>
                <th>Formations</th>
                <th>Lieux</th>
                <th>Prochaine session (début)</th>
                <th>Prochaine session (fin)</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for training in trainings %}          
                <tr>
                    <td>
                        <a href="{{ path('show_training', {id:training.id} ) }}">
                            {{ training.title }}
                        </a>
                    </td>                  
                    <td>{{ training.lieu }} </td>
                    <td></td>
                    <td></td>
                    <td>
                        <!-- https://getuikit.com/docs/icon -->
                        <a class="uk-icon-link uk-margin-small-right" uk-icon="file-edit" href="#"></a>
                        <a class="uk-icon-link" uk-icon="trash" href=""></a>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

{% endblock %}
